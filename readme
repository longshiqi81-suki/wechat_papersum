# WeChat Digest Curator | 微信公众号 AI 周报系统

这是一个基于 **Python**、**Playwright** 和 **OpenAI API** 构建的自动化微信公众号周报系统。该系统能够自动抓取指定公众号的文章，利用 AI 进行内容总结，并生成美观的 HTML 周报通过 163 邮箱推送到指定用户，同时更新网页展示数据。

## 🚀 核心特性

- **自动化抓取**：使用 Playwright 模拟浏览器操作，支持抓取多个目标公众号的过去一周文章。
- **AI 智能总结**：集成 OpenAI GPT 模型，自动提取每篇文章的核心观点和关键信息。
- **邮件推送**：生成响应式 HTML 格式的周报，支持 163 邮箱等 SMTP 服务商推送。
- **数据持久化**：自动生成 `articles.json` 和 `weekly_report.html`，方便集成到静态网页或 Dashboard 展示。
- **容错机制**：当抓取遇到反爬限制时，系统会自动使用演示数据确保总结和推送流程的完整性。

## 🛠️ 技术栈

- **语言**：Python 3.11+
- **自动化**：Playwright (Chromium)
- **AI 模型**：OpenAI GPT-4.1-mini
- **解析**：BeautifulSoup4
- **邮件协议**：SMTP (SSL/TLS)

## 📂 项目结构

```text
wechat_digest/
├── main.py           # 系统入口，协调各模块运行
├── config.py         # 核心配置文件（账号、API Key、邮箱授权码）
├── scraper.py        # 微信公众号文章抓取模块
├── summarizer.py     # AI 总结模块（OpenAI 集成）
├── mailer.py         # 邮件发送模块（SMTP 服务）
├── data/             # 存放生成的 JSON 数据和 HTML 报告
└── logs/             # 存放运行日志
```

## ⚙️ 快速开始

### 1. 克隆项目与安装依赖

```bash
git clone https://github.com/your-username/wechat_digest.git
cd wechat_digest
pip install playwright beautifulsoup4 openai
playwright install chromium
```

### 2. 配置信息

在 `config.py` 中填入您的配置：

- **WECHAT_ACCOUNTS**: 想要关注的公众号名称列表。
- **SENDER_PASSWORD**: 163 邮箱的 SMTP 授权码。
- **OPENAI_API_KEY**: 您的 OpenAI API 密钥。

### 3. 运行系统

```bash
python3 main.py
```

运行完成后，您可以前往 `data/` 目录查看生成的周报，或检查您的收件箱。
